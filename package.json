{
  "name": "profile_app",
  "private": true,
  "dependencies": {
    "@rails/ujs": "^7.1.501",
    "@hotwired/turbo-rails": "^7.3.0",
    "@hotwired/stimulus": "^3.2.1",
    "esbuild": "^0.20.2",
    "jquery": "^3.7.1"
  },
  "scripts": {
    "build": "npx esbuild app/javascript/application.js --bundle --outdir=app/assets/builds --minify"
  }
}
